# AI providers

Providers implement `AiProvider`; selection stays behind ports. OpenAI-compatible configuration accepts `baseUrl` and a BYOK credential. OpenRouter is a configuration of this direction.
